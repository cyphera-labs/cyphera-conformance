{
  "note": "Passthrough preservation matrix — characters NOT in the configured alphabet must round-trip in their exact original positions. This stresses positional bookkeeping across many input shapes. Every SDK must produce identical output and roundtrip.",
  "config": {
    "configurations": {
      "ssn":      { "engine": "ff1", "key_ref": "k1", "header": "T01" },
      "digits_h": { "engine": "ff1", "alphabet": "digits", "key_ref": "k1", "header": "DH1" }
    },
    "keys": {
      "k1": { "material": "2B7E151628AED2A6ABF7158809CF4F3C" }
    }
  },
  "cases": [
    { "configuration": "ssn", "plaintext": "123-45-6789",          "force_method": "access", "note": "single-format SSN, two dashes" },
    { "configuration": "ssn", "plaintext": "(555) 867-5309",       "force_method": "access", "note": "phone — parens + space + dash" },
    { "configuration": "ssn", "plaintext": "1234 5678 9012 3456",  "force_method": "access", "note": "card — spaces every 4" },
    { "configuration": "ssn", "plaintext": "1234-5678-9012-3456",  "force_method": "access", "note": "card — dashes every 4" },
    { "configuration": "ssn", "plaintext": "12/34/5678",           "force_method": "access", "note": "date with slashes" },
    { "configuration": "ssn", "plaintext": "12.34.56.78",          "force_method": "access", "note": "IP-like with dots" },
    { "configuration": "ssn", "plaintext": "-123456789",           "force_method": "access", "note": "leading passthrough" },
    { "configuration": "ssn", "plaintext": "123456789-",           "force_method": "access", "note": "trailing passthrough" },
    { "configuration": "ssn", "plaintext": "--123--456--",         "force_method": "access", "note": "consecutive passthroughs grouped" },
    { "configuration": "ssn", "plaintext": "-a-b-c-d-e-f-g-h",     "force_method": "access", "note": "alternating passthrough/encryptable" },
    { "configuration": "ssn", "plaintext": " abc123 def456 ",      "force_method": "access", "note": "leading + trailing spaces (whitespace pass)" },
    { "configuration": "ssn", "plaintext": "a]b[c{d}e<f>g",        "force_method": "access", "note": "brackets + angles as passthrough" },
    { "configuration": "ssn", "plaintext": "abc'def\"ghi\\jkl",    "force_method": "access", "note": "quotes + backslash" },
    { "configuration": "ssn", "plaintext": "abc&def|ghi;jkl",      "force_method": "access", "note": "shell metacharacters" },
    { "configuration": "ssn", "plaintext": "line1\r\nline2\r\n3",  "force_method": "access", "note": "CRLF line breaks" },
    { "configuration": "ssn", "plaintext": "tab\there\tand\there", "force_method": "access", "note": "tabs as passthrough" },
    { "configuration": "ssn", "plaintext": "​abc123​",              "force_method": "access", "note": "zero-width space passthroughs" },

    { "configuration": "digits_h", "plaintext": "(555) 867-5309",  "force_method": "access", "note": "digits-only alphabet — all alpha treated as passthrough" },
    { "configuration": "digits_h", "plaintext": "$1,234.56",        "force_method": "access", "note": "currency — $ , . all passthrough" },
    { "configuration": "digits_h", "plaintext": "[2026-05-19]",     "force_method": "access", "note": "bracketed date" }
  ]
}
